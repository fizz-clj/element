(ns fizz.element.dev.hmr.registry-test
  (:require [cljs.test :refer [deftest is]]
            [fizz.element.dev.hmr.registry :as reg]))

(deftest test-modifying-registry
  (let [!registry (atom {})]))

